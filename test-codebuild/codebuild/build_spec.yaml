version: 0.2
phases:
  install:
    commands:
      - npm install -g esbuild
  build:
    commands:
      - cd scripts
      - node set-env-variables.js $BRANCH_NAME
      - cd ./test-codebuild
      - sam build
  post_build:
    commands:
      - sam deploy --stack-name test-codebuild
