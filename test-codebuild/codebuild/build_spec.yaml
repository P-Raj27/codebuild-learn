version: 0.2
phases:
  install:
    commands:
      - npm install -g esbuild
  build:
    commands:
      - cd scripts
      - node set-env-variables.js $BRANCH_NAME
      - cd ../backend
      - sam build
  post_build:
    commands:
      - sam deploy
